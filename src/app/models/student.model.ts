export interface Student {
  id?: number;
  name: string;        // or firstName/lastName
  email: string;
  phoneNumber?: string;
  address?: string;
  // Add other fields from your backend
}
